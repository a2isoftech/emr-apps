query WorkRequestOptionsForDepot($filter: DepotQueryFilterInput!) {
    depots(filter: $filter) {
        workRequestOptions {
            serviceLevels {
                id
                name
            }
            requestTypes
        }
    }
}