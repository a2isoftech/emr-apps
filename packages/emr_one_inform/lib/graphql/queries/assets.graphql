query Assets($filter: AssetQueryFilterInput!) {
    assets(filter: $filter) {
        assetCode
        name
        yardCode
        depotNo
        status
        maintenance {
            assetType
            faultSymptoms
        }
    }
}