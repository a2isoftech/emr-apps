query getWorkRequestMedia($cmpCode: String!, $requestId: String!) {
    mediaItemsForWorkRequest(companyCode: $cmpCode, workRequestId: $requestId) {
    id mediaType mediaPath thumbnailPath isUploaded createdBy createdDate uploadDate
    }
}