query UserOrderBooks($after: String) {
  userOrderBooks(first: 50, after: $after) {
    nodes {
      orderBookTypeId
      orderBookName
      gradeType
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
