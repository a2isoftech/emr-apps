name: emr_one_piweigh
description: EMR Apps piweigh package to define graphql operations in a single reusable place.
version: 3.0.0
homepage: EMROnePiweigh

publish_to: none

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
resolution: workspace

dependencies:
  # path: ^1.9.0

dev_dependencies:
  build_runner: ^2.4.13
  graphql_codegen: ^3.0.1
  json_serializable: ^6.8.0
  test: ^1.24.0
  very_good_analysis: ^6.0.0
