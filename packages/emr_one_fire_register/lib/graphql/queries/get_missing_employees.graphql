query GetMissingEmployees($yardCode: String!, $missingOnDate: DateTime!) {
  missingEmployees(input: { missingOnDate: $missingOnDate, yardCode: $yardCode }) {
    id
    punchId
    employeeName
    subjectType
    missingOnDate
    yardCode
    visitorHost
    visitorPhoneNo
  }
}