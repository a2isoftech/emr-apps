query SearchAdvancesFacets($input: AdvancesSearchInput!) {
  searchAdvancesFacets(input: $input) {
    key
    value {
      name
      values {
        name
        range
        count
        min
        max
        average
      }
    }
  }
}
