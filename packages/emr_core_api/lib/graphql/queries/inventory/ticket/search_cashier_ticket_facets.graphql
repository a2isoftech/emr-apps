query SearchCashierTicketsFacet($input: SearchCashierTicketsInput!) {
  searchCashierTicketsFacet(input: $input) {
    key
    value {
      name
      values {
        name
        range
        count
        min
        max
        average
      }
    }
  }
}
