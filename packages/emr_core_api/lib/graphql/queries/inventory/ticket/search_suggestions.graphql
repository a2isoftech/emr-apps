query searchSuggestions(
  $searchText: String!
  $searchPropertyType: TicketSearchPropertyType!
) {
  searchSuggestions(
    searchText: $searchText
    searchPropertyType: $searchPropertyType
  )
}
