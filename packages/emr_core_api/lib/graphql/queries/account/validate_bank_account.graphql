query ValidateBankAccount($sortCode:String!, $accountNumber:String!, $country:CountryId!){
  validatedBankAccount(sortCode: $sortCode, accountNumber: $accountNumber, country: $country){
    isCorrect
    bank
  }
}