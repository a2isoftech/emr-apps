query SearchRetailAccountsFacet($input: RetailAccountsSearchInput!) {
  searchRetailAccountsFacet(input: $input) {
    key
    value {
      name
      values {
        range
        count
        name
      }
    }
  }
}
