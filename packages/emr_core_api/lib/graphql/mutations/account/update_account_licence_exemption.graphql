mutation UpdateAccountLicenseExemption(
  $input: UpdateAccountLicenseExemptionInput!
) {
  updateAccountLicenseExemption(input: $input) {
    ... on Account {
      id
    }
    ... on AccountLicenseExemptionError {
      message
    }
  }
}
