mutation CreateDocumentInternal($accountNumber: String!, $input: CreateNewDocumentInput) {
  createDocumentInternal(accountNumber: $accountNumber, input: $input) {
    accountNumber
    errors {
      message
      code
    }
  }
}
