mutation unlockTicket($ticketIdToUnlock: String) {
  lockTicket(
    input: { forceUnlock: true, profileName: "", ticketIdToUnlock: $ticketIdToUnlock }
  ) {
    unlockResponse {
      isSuccess
      ticketId
      lockedBy
    }
  }
}