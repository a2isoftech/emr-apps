query GetTerritoryDocumentSubTypes($territoryId: TerritoryId) {
  territoryDocumentSubType(where: { territoryId: { eq: $territoryId } }) {
    documentSubType {
      id
      description
    }
  }
}
