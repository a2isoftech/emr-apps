mutation StopCameraStream($accessPointId: String!) {
  stopCameraStream(
    accessPointId: $accessPointId    
  ) {
    accessPointId
    isRunning
    durationMinutes
    timeRemaining
    message
  }
}