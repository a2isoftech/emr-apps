mutation AddCameraToAccessPoint($input: SaveAccessPointCameraInput!) {
  addCameraToAccessPoint(input: $input) {
    id
    externalId
    camera {
      cameraName
      ipAddress
      rtspUrl
      snapshotUrl
      username
      createdUtc
    }
  }
}
