enum GridActions { edit, attachments, images, addToBasket }

enum MobileInventoryActions {
  resetScan,
  scanned,
  triage,
  defab,
  cutting,
  measure,
  reprintLabel,
  production,
  finish
}

enum BeamType { ub, uc }
